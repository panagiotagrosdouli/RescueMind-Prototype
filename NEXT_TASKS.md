# Next Tasks

1. Expand scenario descriptors into scenario-specific world generators.
2. Add explicit hypothesis split, merge, retirement, and revision-vector handling.
3. Add logistic and isotonic calibration on committed synthetic train/calibration/test splits.
4. Add a local interactive dashboard with operator confirmation, rejection, override, and evidence requests.
5. Implement and validate a ROS2 bridge in a ROS2 Humble or Jazzy environment.
6. Validate the Docker image with an available daemon and archive the exact build/run logs.
7. Add external dataset adapters without claiming dataset validation until they are actually executed.
8. Add physical robot interfaces only after hardware becomes available.
