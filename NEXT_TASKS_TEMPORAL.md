# Temporal Alignment Follow-up

- Estimate clock offsets online from synchronization exchanges.
- Add interpolation only for continuous measurements with explicit uncertainty growth.
- Integrate the asynchronous buffer into the main simulation pipeline.
- Validate ROS2 time and rosbag replay behaviour when a ROS2 runtime is available.
- Benchmark alignment quality under controlled delay, loss, and reordering.
